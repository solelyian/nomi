<Window
    x:Class="Nomi.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Nomi">
    <Grid x:Name="Root" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.KeyboardAccelerators>
            <KeyboardAccelerator Key="K" Modifiers="Control" Invoked="CommandInvoked" />
        </Grid.KeyboardAccelerators>
        <NavigationView
            x:Name="Navigation"
            PaneDisplayMode="Auto"
            OpenPaneLength="232"
            CompactModeThresholdWidth="720"
            ExpandedModeThresholdWidth="1100"
            IsSettingsVisible="False"
            IsBackButtonVisible="Collapsed"
            SelectionChanged="NavigationChanged">
            <NavigationView.PaneHeader>
                <TextBlock Text="nomi." FontSize="34" FontWeight="SemiBold" Margin="18,24,0,30" />
            </NavigationView.PaneHeader>
            <NavigationView.PaneFooter>
                <StackPanel Spacing="8" Margin="18,24">
                    <TextBlock x:Name="BrandCaption" TextWrapping="Wrap" FontSize="12" />
                    <TextBlock Text="NYNE TECHNOLOGIES" FontSize="10" CharacterSpacing="120" />
                </StackPanel>
            </NavigationView.PaneFooter>
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid x:Name="HeaderLayout" Margin="24,18" ColumnSpacing="16" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <VisualStateManager.VisualStateGroups>
                        <VisualStateGroup>
                            <VisualState>
                                <VisualState.StateTriggers>
                                    <AdaptiveTrigger MinWindowWidth="0" />
                                </VisualState.StateTriggers>
                                <VisualState.Setters>
                                    <Setter Target="HeaderControls.(Grid.Row)" Value="1" />
                                    <Setter Target="HeaderControls.(Grid.Column)" Value="0" />
                                    <Setter Target="HeaderControls.HorizontalAlignment" Value="Left" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState>
                                <VisualState.StateTriggers>
                                    <AdaptiveTrigger MinWindowWidth="850" />
                                </VisualState.StateTriggers>
                                <VisualState.Setters>
                                    <Setter Target="HeaderControls.(Grid.Row)" Value="0" />
                                    <Setter Target="HeaderControls.(Grid.Column)" Value="1" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateManager.VisualStateGroups>
                    <TextBlock x:Name="Breadcrumb" VerticalAlignment="Center" TextWrapping="Wrap" FontSize="13" />
                    <StackPanel x:Name="HeaderControls" Grid.Column="1" Orientation="Horizontal" Spacing="14">
                        <Button x:Name="CommandButton" Content="Ctrl+K" Click="OpenCommandClicked" MinHeight="40" VerticalAlignment="Bottom" />
                        <ComboBox x:Name="LanguagePicker" SelectionChanged="LanguageChanged" MinWidth="130">
                            <ComboBoxItem Content="Français" Tag="fr" />
                            <ComboBoxItem Content="English" Tag="en" />
                        </ComboBox>
                    </StackPanel>
                </Grid>
                <ScrollViewer x:Name="PageScroll" Grid.Row="1" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="PageBody" MaxWidth="980" HorizontalAlignment="Stretch" Margin="28,24,28,36" Spacing="20" />
                </ScrollViewer>
                <Border Grid.Row="2" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="24,14">
                    <StackPanel Spacing="6">
                        <TextBlock x:Name="PrototypeLabel" FontSize="12" TextWrapping="Wrap" />
                        <TextBlock x:Name="PrototypeNote" FontSize="12" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
